<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="aStudent"
            type="com.databinding.MainActivity.Student" />

        <variable
            name="handleClick"
            type="com.databinding.MainActivity.EventHandler" />
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:orientation="vertical"
        tools:context=".MainActivity">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@{aStudent.name}" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@{String.valueOf(aStudent.age)}" />

        <Button
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:onClick="@{()->handleClick.handlerEvent(aStudent.name)}"
            android:text="Click" />
        <!--android:onClick="@{()->handleClick.handlerEvent()}"-->
        <!--android:onClick="@{(view)->handleClick.handlerEvent(view)}"-->
        <!-- android:onClick="@{handleClick::handlerEvent}"-->
        <!-- android:onClick="@{handleClick.handlerEvent}"-->
    </LinearLayout>
</layout>